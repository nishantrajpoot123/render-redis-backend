services:
  - type: web
    name: your-service-name
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app  # or whatever your start command is
    runtime: python-3.10.12
